<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />

    <meta-data
        android:name="com.google.android.gms.version"
        android:value="@integer/google_play_services_version" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Ad_gk"
        tools:targetApi="31">

        <!-- LoginActivity là màn hình đầu tiên -->
        <activity
            android:name=".activity.MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Các Activity khác -->
        <activity
            android:name=".activity.UserDetailActivity"
            android:exported="false" />
        <activity
            android:name=".activity.EditUserActivity"
            android:exported="false" />
        <activity
            android:name=".activity.CertificateDetailActivity"
            android:exported="false" />
        <activity
            android:name=".activity.EditCertificateActivity"
            android:exported="false" />
        <activity
            android:name=".activity.StudentDetailActivity"
            android:exported="false" />
        <activity
            android:name=".activity.EditStudentActivity"
            android:exported="false" />
        <activity
            android:name=".activity.AddCertificateActivity"
            android:exported="false" />
        <activity
            android:name=".activity.AddStudentActivity"
            android:exported="false" />
        <activity
            android:name=".activity.AddUserActivity"
            android:exported="false" />
        <activity
            android:name=".activity.LoginActivity"
            android:exported="false" />
    </application>
</manifest>
