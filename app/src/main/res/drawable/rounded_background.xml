<?xml version="1.0" encoding="utf-8"?>
<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="rectangle">

    <!-- Màu nền -->
    <solid android:color="@color/bluewith"/>
    <!-- Bo góc -->
    <corners android:radius="16dp" />
    <!-- Viền (nếu cần) -->
    <stroke
        android:width="1dp"
        android:color="#DDDDDD" />
</shape>
